# app/data/options — ships empty by design

This directory ships EMPTY of data files — a README only (OPTION_FORMAT_SPEC.md
§0 structural guard 1, §13). Real data arrives via the harvest stage. No
sample, seed, starter, or demo data files may be placed here; illustrative
data lives only in `tests/fixtures/`.
