# app/data/age_bands.json — ships empty by design

The age-band lookup file `app/data/age_bands.json` (OPTION_FORMAT_SPEC.md §8)
ships empty-of-data — this README only. Its content arrives at the harvest
stage. No sample, seed, starter, or demo data may be placed here; illustrative
data lives only in `tests/fixtures/`.
