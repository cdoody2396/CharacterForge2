# O3 INPUTS amendment — the N8 grade ladder

Delivered by the planning gate (Casey) in-chat on 2026-07-22, AFTER the O3
push: the O3 session's one NOT_DECIDED (SESSION_REPORT_O3). Committed here
verbatim so the contract lives in the repo. Everything below the line is
the delivery, untouched.

---

N8 GRADE LADDER — verbatim from CHECKPOINT_creation-scoping_S1_2026-07-21_AMENDED
(project knowledge; not in repo). [AR] markers are adversarial-review amendments
and carry decided force per the checkpoint's marking conventions. The ring
derivation rule remains an owed deliverable (image-identity section).

### 1c — Canonical set structure (approved as structure)

- Canonical set = two named subsets, one finalization pass (the pass runs at
  whichever finalization first selects G1+, including later upgrades):
  - **Reference core** — neutral fixed framings: **[AR — disambiguation owed
    before the ring-derivation session:** "LoRA seeds" has two readings — (a)
    the core *is* the training set, which collides with the 15–30 vetted floor
    and the retained bootstrap+cull mechanics, or (b) the core *seeds* the
    retained bootstrap→cull→confirm path. The salvage set implies (b); pin it
    explicitly in the image-identity section.**]** Plus per-framing IP-Adapter
    references.
  - **Presentation ring** — likely zoom-ins + expression fan: card imagery,
    depicted-tier answers. Ring frames are variable-bearing renders under 1b —
    they record their generation variables and can go stale on persona edits.
- Ring membership derives from record contents, not a fixed list — the
  derivation rule is an owed deliverable.

### Identity grades

| Grade | Contents | Playability |
|---|---|---|
| G0 — basic | approved portrait + IP-Adapter reference | fully playable immediately; all imagery on-demand via adapter. **[AR caveat:** unvalidated for exotic anatomy under variables-only prompts — adapter is documented weakest there and every prior hardware result had identity fragments in the prompt. Carve-out question owed to image-identity.**]** |
| G1 — canonical | G0 + canonical shot set (reference core + presentation ring, one pass) | depicted answers and card imagery pre-rendered |
| G2 — anchored | G1 + identity LoRA(s) trained from the reference core | strongest consistency; covers the exotic-anatomy end where the adapter weakens |

- G0 is the floor of every character; grade choice presented at finalization;
  any grade reachable later as a background upgrade job. Strong.
- Artifact ledger on the identity layer. **[AR amendment:** the field list is
  decided-as-*minimum*, not exhaustive: kind, identity version, method, active
  flag, **generation-variable provenance, stale marker** (required by 1b).
  `grade` is **not** stored per entry — it is a rollup derived from ledger
  contents (has-canonical-set, has-active-LoRA); a stored per-entry grade can
  contradict the derived one. Multiple LoRAs per version, one active; later
  grades append without schema change. Strong.**]**
- Depicted is a routing rule, not an existence guarantee — G0 routes depicted
  queries to on-demand generation; no consumer may assume G1+. Strong. (The
  G0-exotic caveat bites exactly here.)
- G2 presupposes the reference core; ring-skip-to-LoRA is an image-section
  execution call.
- Card renders from the ledger; G0 card is portrait-led with an upgrade
  affordance (step-2 contract note).
